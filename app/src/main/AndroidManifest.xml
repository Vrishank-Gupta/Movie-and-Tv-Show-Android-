<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vrishankgupta.movies">


    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".welcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Option" />
        <activity android:name=".MovieMain" />
        <activity android:name=".TvShowMain" />
        <activity android:name=".MovieOption" />
        <activity android:name=".TvOption" />
        <activity android:name=".MovieDetail" />
        <activity android:name=".TvDetail" />
        <activity android:name=".SearchActivity"></activity>
    </application>

</manifest>